export type CardTypes = {
  title: string;
  amount: string;
  idNumber: string;
  backgroundColor: string;
};

export type ListItemTypes = {
  title: string;
  description: string;
  amount: string;
  when: string;
  image: string;
};
